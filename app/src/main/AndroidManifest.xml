<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".image_view.ui.profile_image"></activity>

        <activity android:name=".about" />
        <activity android:name=".user_profile" />
        <activity
            android:name=".chatmassage"
            android:label="@string/title_activity_chatmassage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".all_user" />
        <activity android:name=".prof" />
        <activity
            android:name=".loginAc"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".regirester"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".DashBoard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>